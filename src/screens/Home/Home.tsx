import { Box } from "@mui/material";
import { Link } from "react-router-dom";

const Home = () => {
  return (
    <Box>
      <Link to={"/login"}>dasd</Link>
    </div>
  );
};

export default Home;
