import { Box } from "@mui/material";
import { Link } from "react-router-dom";

const Home = () => {
  return (
    <Box>
      <Link to={"/login"}>dasd</Link>
    </Box>
  );
};

export default Home;
